name: Building-blocks

on: push

jobs:
     echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello, Thenz"
     echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed step
              run: |
                 exit 1
            - name: Say goodbye
              run: echo "Goodbye"
